export type SearchType = "phone" | "receipt";
export type DateType = {
  year: string;
  month: string;
  day?: string;
};
export type FormOption = {
  id: string;
  label: string;
};

export type LineTxt = {
  label: string;
  value: string;
};
