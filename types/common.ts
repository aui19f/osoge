export type SearchWordTarget = "phone" | "receipt";

export type DateFields = {
  year: string;
  month: string;
  day?: string;
};

export type FormOption = {
  id: string;
  label: string;
};

export type LineTxt = {
  label: string;
  value: string;
};
